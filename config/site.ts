export type SiteConfig = typeof siteConfig;

export const siteConfig = {
    name: "Unite",
    description:
        "Unite is a powerful data etl tool that helps you to extract, transform and load data from various sources.",
};
